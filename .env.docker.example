# Docker / VPS deployment env (copy to .env)
APP_ENV=production
DEBUG=false
SECRET_KEY=change-me-to-a-random-secret-key-at-least-32-chars
API_V1_PREFIX=/api/v1
APP_HTTP_PORT=8082

# Postgres (service name inside compose network)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=afg_user
POSTGRES_PASSWORD=change-me-postgres-password
POSTGRES_DB=academic_figure_generator

# Redis (service name)
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# MinIO (service name)
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=change-me-minio-secret
MINIO_BUCKET_NAME=academic-figures
MINIO_USE_SSL=false

# JWT
JWT_SECRET_KEY=${SECRET_KEY}
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Encryption (32-byte hex)
ENCRYPTION_MASTER_KEY=change-me-to-a-32-byte-hex-key

# NanoBanana API
# 平台统一 Key（不填则必须在用户设置里填写 BYOK）
NANOBANANA_API_KEY=
# 兼容自建 / 第三方网关（默认即可）
NANOBANANA_API_BASE=https://api.keepgo.icu

# CORS
CORS_ORIGINS=["http://localhost","http://localhost:80","http://YOUR_VPS_IP"]

# File Upload
MAX_UPLOAD_SIZE_MB=50
